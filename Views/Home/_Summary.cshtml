@model Restaurant

<section>
    <h3>@Model.Name</h3>
    <div>
        @Model.Cuisine
    </div>
    <div>
        <a asp-action="Details" asp-route-id="@Model.Id">檢視</a>
        <a asp-action="Edit" asp-route-id="@Model.Id">編輯</a>
    </div>
</section>